# Extension Icons

Place PNG icon files here:

- `icon16.png` — 16×16px (browser toolbar)
- `icon48.png` — 48×48px (extensions management page)
- `icon128.png` — 128×128px (Chrome Web Store)

These files are referenced in `src/manifest.json`. Generate them from an SVG source in Phase 2.
